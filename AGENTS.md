# Project Development Guidelines

## Versioning Policy
- **Mandatory Version Bumps**: Whenever making changes or fulfilling user requests, you MUST increment the `versionCode` (integer) and update the `versionName` (e.g. 1.0 -> 1.1 -> 1.2) in `app/build.gradle.kts`.
